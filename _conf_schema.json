{
  "timeout_seconds": {
    "description": "飞花令限时（秒）",
    "type": "int",
    "hint": "每轮飞花令的限时，默认 90 秒。超时未接龙将自动跳过或结束游戏。",
    "default": 90
  },
  "data_download_url": {
    "description": "数据库下载地址",
    "type": "string",
    "hint": "poetry_data.zip 下载地址。留空则不自动下载。首次使用建议手动上传 poetry_data.db 到数据目录",
    "default": ""
  }
}
